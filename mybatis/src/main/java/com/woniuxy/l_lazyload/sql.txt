CREATE TABLE dept
( 
  did INT PRIMARY KEY AUTO_INCREMENT,
  dname VARCHAR(20)
);

CREATE TABLE emp
( 
  eid INT PRIMARY KEY AUTO_INCREMENT,
  ename VARCHAR(20),
  did INT
);

ALTER TABLE emp
ADD CONSTRAINT FK2 FOREIGN KEY(did)
REFERENCES dept(did);